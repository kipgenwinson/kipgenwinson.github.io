source "https://rubygems.org"

# GitHub Pages compatible build. This mirrors what GitHub builds for you.
gem "github-pages", group: :jekyll_plugins

# For local previewing
gem "webrick"

platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end
